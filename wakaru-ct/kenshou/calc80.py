# -*- coding: utf-8 -*-
# 深掘り⑳：微細構造定数は、重力で変わるのか
#   ① α は何と何の比なのか（質量もħもcも消える）
#   ② 一般相対論の答え／③ 見かけの変化（赤方偏移）との区別
#   ④〜⑦ 破れを許したときの上限を、三通りの実験で
#   ⑧⑨ GR が実際に与える本物の効果の大きさ
import math

# --- CODATA ---
alpha = 7.2973525693e-3
hbar = 1.054571817e-34
c = 2.99792458e8
me = 9.1093837015e-31
e = 1.602176634e-19
G = 6.67430e-11
a0 = 5.29177210903e-11
lam_C = hbar / (me * c)          # 換算コンプトン波長
r_e = alpha * lam_C              # 古典電子半径
Ry = 2.1798723611e-18            # J

print("=== ① α は「長さと長さの比」だった ===")
print("  ご指摘のとおり α は質量・ħ・c と結びついています。ただし ──")
print("  それらを組み合わせると、**全部打ち消えます**。")
print("")
print("  換算コンプトン波長 ƛ_C = ħ/(m c) = %.6e m" % lam_C)
print("  ボーア半径　　　　 a₀   = ƛ_C/α    = %.6e m" % a0)
print("  古典電子半径　　　 r_e  = α ƛ_C    = %.6e m" % r_e)
print("")
print("  %-22s %-16s %s" % ("比", "値", "＝"))
print("  %-22s %-16.9f %s" % ("a₀ / ƛ_C", a0 / lam_C, "**1/α = %.6f**" % (1 / alpha)))
print("  %-22s %-16.9f %s" % ("ƛ_C / r_e", lam_C / r_e, "**1/α**"))
print("  %-22s %-16.9e %s" % ("r_e / a₀", r_e / a0, "**α² = %.6e**" % alpha ** 2))
print("")
print("  ★ **r_e : ƛ_C : a₀ = α² : α : 1**")
print("     三つの長さの梯子で、段の比がすべて α。")
print("  ★ そして **m も ħ も c も、比を取ると消えます**。")
print("     α = r_e/ƛ_C の右辺には、質量が入っていません。")
print("     → **α は「どの単位で測っても同じ数」＝ 重み 0 の量**。")

print("")
print("=== ② 一般相対論の答えは「厳密に変わらない」 ===")
print("  GR は計量理論で、**アインシュタインの等価原理**を満たします。")
print("  その一部が **局所位置不変性（LPI）**：")
print("    「局所的な非重力実験の結果は、いつ・どこで行っても同じ」")
print("")
print("  ★ **これは α についての直接の言明です。**")
print("     局所的に測った α は、重力ポテンシャルに依らない ── **予言は厳密にゼロ**。")
print("  ★ つまり『重力で α が変わる』は、GR の中では **定義により起きません**。")
print("     起きたら、GR の土台（等価原理）が壊れます。")
print("     → だからこれは **GR の検証実験**になります。")

print("")
print("=== ③ ただし『見かけの変化』はある ── そこを区別する ===")
print("  重力井戸の底の原子が出した光は、遠くで**赤方偏移**して届きます。")
print("  素朴に見ると「振動数が変わった＝原子の性質が変わった」に見える。")
print("")
tests = [
    ("重力赤方偏移", "**全部の線が同じ比率でずれる**", "重み ≠ 0（遠くの時計との比）",
     "単位の取り替えで消える"),
    ("**α の変化**", "**線ごとに違う量だけずれる**", "**重み 0（同じ場所の二つの時計の比）**",
     "**消せない**"),
]
print("  %-18s %-34s %-34s %s" % ("現象", "見え方", "本シリーズの分類", "ゲージで"))
for a, b, cc, d in tests:
    print("  %-18s %-34s %-34s %s" % (a, b, cc, d))
print("")
print("  ★ 区別する鍵は **遷移ごとの α 感度 K**（ν ∝ α^K）：")
clocks = [("Al⁺", 0.008), ("Yb⁺ (E2)", 1.00), ("Cs 超微細", 0.83),
          ("Hg⁺ (E2)", -2.94), ("Yb⁺ (E3)", -5.95), ("Dy（縮退準位）", 'huge')]
print("  %-22s %s" % ("遷移", "K"))
for a, k in clocks:
    print("  %-22s %s" % (a, k if isinstance(k, str) else "%+.2f" % k))
print("")
print("  ★ 赤方偏移なら **K に依らず同じ**ずれ。α の変化なら **ΔK 倍**違う。")
print("     → **同じ場所に置いた二つの時計の比**を見れば、赤方偏移は完全に消える。")

print("")
print("=== ④ 破れを許したときの、標準的な書き方 ===")
print("  等価原理を破る理論（スカラー・テンソル、ディラトン等）では、")
print("  α が局所の重力ポテンシャル Φ に依ってよい：")
print("")
print("      **Δα/α = k_α · ΔΦ/c²**")
print("")
print("  k_α = 0 が GR。**k_α を測るのが、この分野の中心**です。")

print("")
print("=== ⑤ ポテンシャル差は、どこでどれだけ取れるか ===")
GMsun_c2 = 1476.6      # m
GMe_c2 = 4.4350e-3     # m
AU = 1.495978707e11
Re = 6.371e6
Rsun = 6.957e8
places = [
    ("地球の表面（地球の重力）", GMe_c2 / Re),
    ("地球の位置（太陽の重力）", GMsun_c2 / AU),
    ("**地球の公転による年周変化**", 2 * 0.0167 * GMsun_c2 / AU),
    ("太陽の表面", GMsun_c2 / Rsun),
    ("**白色矮星の表面**（0.6M☉, 8500 km）", 0.6 * GMsun_c2 / 8.5e6),
    ("中性子星の表面（1.4M☉, 12 km）", 1.4 * GMsun_c2 / 1.2e4),
]
print("  %-40s %s" % ("場所", "Φ/c²"))
for nm, v in places:
    print("  %-40s %.3e" % (nm, v))
print("")
print("  ★ **地上の実験で使えるのは、公転による年周変化 3.3×10⁻¹⁰ だけ**。")
print("     地球の重力は動かせないので、差が取れない。")

print("")
print("=== ⑥ 測り方その一：原子時計の年周変化 ===")
dPhi = 2 * 0.0167 * GMsun_c2 / AU
dK = 5.95 + 1.00
clock_stab = 1e-17
print("  Yb⁺ の E3/E2 なら ΔK = %.2f" % dK)
print("  k_α = 1 なら、年周で 比が **%.1e** 動くはず" % (dK * dPhi))
print("  時計の比の安定度（1 年平均）≈ %.0e" % clock_stab)
print("  → 原理的な感度 **k_α ≈ %.1e** まで見える" % (clock_stab / (dK * dPhi)))
print("")
print("  ★ 実際に公表されている上限は **|k_α| ≲ 10⁻⁶〜10⁻⁸**")
print("     （系統誤差と、1 年という長い時間の安定性が効くため、原理限界には届かない）")

print("")
print("=== ⑦ 測り方その二・その三 ── まったく別の実験で、もっと強い ===")
print("  ● 白色矮星のスペクトル")
wd = 0.6 * GMsun_c2 / 8.5e6
wd_meas = 1e-5
print("    ΔΦ/c² = %.1e ── 地上の年周変化の **%.0e 倍**" % (wd, wd / dPhi))
print("    Fe V / Ni V 線から |Δα/α| ≲ %.0e" % wd_meas)
print("    → **k_α ≲ %.1e**" % (wd_meas / wd))
print("    ★ 上限は弱いが、**桁違いに深い重力井戸**を試している（非線形項に効く）")
print("")
print("  ● 等価原理の試験（いちばん強い）")
print("    α が Φ に依るなら、**電磁的な束縛エネルギーの割合が違う物体は違う落ち方をする**。")
aC = 0.71   # MeV、クーロン項の係数
u = 931.494


def coul_frac(Z, A):
    return aC * Z * (Z - 1) / A ** (4.0 / 3.0) / u


ti = coul_frac(22, 48)
pt = coul_frac(78, 195)
print("    クーロン束縛エネルギーの割合（E_C/Mc²、液滴模型）：")
print("      チタン（Z=22, A=48）  %.3e" % ti)
print("      白金　（Z=78, A=195） %.3e" % pt)
print("      差 Δ = **%.2e**" % abs(pt - ti))
eta = 1e-15
print("    MICROSCOPE の上限 η < %.0e" % eta)
print("    → **k_α ≲ %.1e**" % (eta / abs(pt - ti)))
print("")
print("  ★ **同じ一つの k_α が、三つの無関係な実験で縛られている。**")
print("     時計（年周）／白色矮星（深い井戸）／自由落下（等価原理）")
print("     ── ペランの 13 通りと同じ形。そして **等価原理がいちばん強い**。")

print("")
print("=== ⑧ では、GR が実際に与える『本物の』効果はどれくらいか ===")
print("  α は変わらなくても、重力は原子に**確かに**効きます。ただし潮汐として。")
tidal = 3.986e14 / Re ** 3          # GM/r³ [s⁻²]
dE = me * tidal * a0 ** 2
print("")
print("  ● 潮汐効果（原子の大きさ a₀ にわたる重力勾配）")
print("    GM/r³（地表）= %.3e s⁻²" % tidal)
print("    ΔE ~ m_e (GM/r³) a₀² = %.3e J" % dE)
print("    リュードベリ 2.180e-18 J に対して **%.2e**" % (dE / Ry))
print("    ★ **10⁻³⁹**。原子は重力の勾配をほとんど感じない。")
print("")
print("  ● 真空偏極による光速の曲率補正（Drummond–Hathrell, 1980）")
Rcurv = tidal / c ** 2
dh = alpha * lam_C ** 2 * Rcurv
print("    曲率 R ~ (GM/r³)/c² = %.3e m⁻²" % Rcurv)
print("    δc/c ~ α ƛ_C² R = **%.2e**" % dh)
print("    ★ **10⁻⁵⁰**。ただし ── **これは本物の「c が二つになる」効果**。")
print("       偏光によって光速がわずかに違う。原理的には測れる量。")

print("")
print("=== ⑨ 大きさの見取り図 ===")
scale = [
    ("重力赤方偏移（地表、遠方比）", GMe_c2 / Re, "**α の変化ではない**（重み ≠ 0）"),
    ("公転による Φ の年周変化", dPhi, "k_α を測る舞台"),
    ("α の変化の上限（時計、年周）", 1e-17, "**測定の壁**"),
    ("潮汐による原子準位のずれ", dE / Ry, "**GR の本物の効果**"),
    ("真空偏極による δc/c", dh, "**二つ目の c**"),
    ("重力の微細構造定数 α_G", G * me ** 2 / (hbar * c), "電磁気との比"),
]
print("  %-38s %-14s %s" % ("量", "大きさ", "身分"))
for nm, v, note in scale:
    print("  %-38s %-14.2e %s" % (nm, v, note))

print("")
print("=== ⑩ 答え ===")
ans = [
    ("「重力があると α が変わるか」", "**GR では厳密にノー**", "局所位置不変性そのもの"),
    ("「観測で変化が見えるか」", "**見えない**", "k_α ≲ 10⁻¹² まで縛られている"),
    ("「見かけの変化はあるか」", "**ある（赤方偏移）**", "**ただし α の変化ではない**"),
    ("「GR は原子に効かないのか」", "**効く（潮汐）**", "ただし 10⁻³⁹"),
    ("「c が変わることはあるか」", "**ある（真空偏極）**", "**二つの c の比として。10⁻⁵⁰**"),
]
print("  %-32s %-22s %s" % ("問い", "答え", "中身"))
for a, b, cc in ans:
    print("  %-32s %-22s %s" % (a, b, cc))
print("")
print("  ★ **鍵は最初の①でした** ── α は長さと長さの比で、m も ħ も c も消える。")
print("     **重み 0 の量は、重力ポテンシャルでは動かない**（等価原理がそう言っている）。")
print("     動くように見えるのは、**重み ≠ 0 の量（振動数そのもの）**の方。")
print("  ★ そして『動くかどうか』は好みの問題ではなく、**三つの実験で測ってある**。")

print("")
print("=== ⑪ 正直な線 ===")
print("  ・⑥⑦の実験上限（10⁻⁶〜10⁻⁸、10⁻¹²）は**文献値を桁で引用**したもので、")
print("    本稿では再計算していません。**桁の話としてのみ読んでください**。")
print("    自分で計算したのは、ポテンシャル差・クーロン束縛割合・潮汐・δc/c です。")
print("  ・⑦の等価原理からの k_α 上限は、**k_α と物質組成の結合を単純化**しています。")
print("    実際には α だけでなく他の定数も同時に動く模型が多く、係数が変わります。")
print("  ・⑧の Drummond–Hathrell は係数を α ƛ_C² R と粗く置いた**次元解析**です。")
print("    正確には 1/(90π) 程度の因子と、偏光・伝播方向への依存が付きます。")
print("  ・②の『GR では厳密にゼロ』は、**GR が正しければ**の話。")
print("    等価原理は仮定であって定理ではなく、だからこそ⑥⑦で測っています。")
print("  ・そして ── **「α が変わる」と「Φ に依る」は同じではありません**。")
print("    時間変化（宇宙論的な走り）は別の問い（深掘り⑯）で、上限も別です。")
