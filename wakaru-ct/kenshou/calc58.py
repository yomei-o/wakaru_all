# -*- coding: utf-8 -*-
# 深掘り③：圧縮はどこから来たのか ── 番外編②の解釈を詰め直す
import math

ln2 = math.log(2)
bic = 0.5 * math.log2(1701)

MP = 1.220890e19
v = 246.22
LQ = 0.332

print("=== ① まず、番外編②の主張を疑ってみる ===")
print("  番外編②：『B ビットの階層は、指数写像を通せば log₂B ビットで買える』")
print("  では聞くが ── **その圧縮は、どこから来たのか。**")
print("")
print("  Λ = f(α)、利得 G = d lnΛ/d lnα とする。")
print("  α が ln で幅 R_α だけ動けば、Λ は ln で幅 G·R_α だけ動く。")
print("  → **写像が全単射なら、Λ の事前範囲は R_Λ = G·R_α に決まってしまう。**")
print("  そのとき：")
print("    直接 Λ を書く値段     = log₂(R_Λ / 精度)")
print("    α 経由で書く値段      = log₂(R_α / (精度/G)) = log₂(R_α·G / 精度)")
print("    差 = log₂(R_Λ / (R_α·G)) = log₂(G·R_α / (R_α·G)) = **0**")
print("  ★ **写像そのものからは、圧縮は 1 ビットも出ない。**")
print("     ── 番外編②の 47 倍は、写像から出たのではなかった。")

print("")
print("=== ② では、どこから出たのか ===")
print("  実際に使ったのは、非対称な二つの事前分布：")
pri = [
    ("ρ_Λ/ρ_Planck", "対数一様、およそ 123 桁", "**上限も下限も無い**", 408.4),
    ("結合定数 α", "[0, 1] 程度", "**O(1) に押し込められている**", math.log2(408.4)),
]
print("  %-22s %-24s %-24s %s" % ("量", "事前範囲", "根拠", "値段[bit]"))
for a, b, c, d in pri:
    print("  %-22s %-24s %-24s %.2f" % (a, b, c, d))
print("")
print("  ★ **圧縮の出どころは、写像ではなく『α の事前分布が O(1) に縛られていること』。**")
print("     結合定数が O(1) なのには理由がある ── 摂動性、漸近自由性、")
print("     そして無次元だから単位の選び方で動かせないこと（第3回・第47回）。")
print("     スケールの比には、そういう理由が無い。")

print("")
print("=== ③ 核心 ── 第二の基準は、第一の基準に潰れる ===")
print("  第48回の基準： **事前分布に理由があるか**")
print("  番外編②の基準：**指数写像が使えるか**")
print("")
print("  ①②より、指数写像が効くのは")
print("    **『事前分布に理由がある量（結合定数）へ、問いを移し替えられる』からだった。**")
print("  → **二つの基準は独立ではない。第二は第一の一つの実装。**")
print("  ★ これは**五度目の圧縮**（第26・40・41・46回に続く）── 今度は自分の基準に当てた。")

print("")
print("=== ④ 言い直すと、基準は一つ ===")
print("  **機構が階層を説明したと言えるのは、")
print("    『事前分布に理由がある量』に問いが着地したときだけ。**")
print("  指数写像は、そこへ効率よく運ぶ道具にすぎない。")

print("")
print("=== ⑤ この基準で、実際の機構を採点する ===")
mech = [
    ("次元転移（QCD）", "α_s（結合定数）", "**ある**：摂動性で O(1)", "◎ 解けている", "実際、誰も問題視しない"),
    ("シーソー（ニュートリノ質量）", "M（新しいスケール）", "無い：スケールに上限が無い", "△ 移しただけ", "実際、M の起源が問われる"),
    ("フロッゲット–ニールセン", "ε = ⟨φ⟩/M（比）", "無い：これもスケール比", "△ 移しただけ", "実際、記述であって説明でないと言われる"),
    ("テクニカラー型", "α_hidden（結合定数）", "**ある**", "◎ 原理的には解ける", "実際、機構としては筋がよいとされた"),
    ("人間原理／多宇宙", "ρ_Λ そのもの", "**事前分布を作り替える**", "── 別の手", "実際、これは第一の基準を直接いじる手"),
]
print("  %-26s %-22s %-26s %-16s %s" % ("機構", "問いの着地先", "その量の事前に理由は", "採点", "実際の扱われ方"))
for a, b, c, d, e in mech:
    print("  %-26s %-22s %-26s %-16s %s" % (a, b, c, d, e))
print("")
print("  ★ **採点と、実際の扱われ方が一致する。**")
print("     とくにシーソーとフロッゲット–ニールセンが『移しただけ』に落ちるのが効いている ──")
print("     どちらも計算としては成功しているのに、**説明としては半分**だと言われてきた。")
print("     その理由が『着地先の事前分布に理由が無い』で言い切れる。")

print("")
print("=== ⑥ ただし、三つ目の手がある ===")
print("  ⑤で人間原理は『事前分布を作り替える』手だった。もう一つある：")
print("  **緩和（relaxation）── パラメータを動的にして、勝手に落ち着かせる。**")
relax = [
    ("ペッチェイ–クイン（アクシオン）", "θ を場にする", "θ → 0 に落ちる", "強い CP"),
    ("クインテッセンス／コスモン", "ρ_Λ を場にする", "トラッカー解に落ちる", "宇宙定数"),
]
print("  %-30s %-20s %-22s %s" % ("機構", "やること", "落ち着き先", "対象"))
for a, b, c, d in relax:
    print("  %-30s %-20s %-22s %s" % (a, b, c, d))
print("  ★ 緩和は**事前分布を変えず、写像も使わない** ── パラメータを消してしまう手。")
print("     第5回の帳簿では『パラメータ 1 個減って、場が 1 個増える』。")

print("")
print("=== ⑦ 手は三つしかない ===")
moves = [
    ("① 事前分布を作り替える", "人間原理・多宇宙・選択効果", "第48回の基準そのもの"),
    ("② 事前に理由がある量へ移す", "次元転移・大統一", "番外編②（＝①の実装）"),
    ("③ パラメータを動的にして緩和する", "アクシオン・クインテッセンス", "パラメータを消す"),
]
print("  %-30s %-30s %s" % ("手", "例", "本シリーズの言葉で"))
for a, b, c in moves:
    print("  %-30s %-30s %s" % (a, b, c))
print("")
print("  ★ **宇宙定数問題の論争は、この三つの陣営そのもの。**")
print("     人間原理（①）、指数機構（②）、クインテッセンス（③）── それ以外が無い。")
print("     ── 基準を作ってから見に行ったら、陣営の数まで合っていた。")

print("")
print("=== ⑧ では、番外編②の 47 倍は間違いだったのか ===")
B = 408.4
print("  **算術としては正しい。** B = %.1f ビットに対し log₂B = %.2f ビット。" % (B, math.log2(B)))
print("  変わるのは**解釈**：")
print("    ✗ 旧：『指数写像そのものが 47 倍の圧縮を生む』")
print("    ○ 新：『指数写像は、事前に理由がある量へ問いを移す。")
print("           圧縮の実体は、その量の事前範囲が O(1) だということ。』")
print("  ★ 数字は動かない。**言明の主語が変わる。**")
print("     ── 第3回の作法そのもの：**比較相手（ここでは事前分布）を言わなければ、")
print("        まだ文になっていない。** 番外編②はそこが甘かった。")

print("")
print("=== ⑨ 副産物 ── 利得は、二つの顔を持つ ===")
G_qcd = math.log(MP / LQ)
G_mu = 2 * math.pi / (7.667 * 0.1180)
print("  同じ利得 G が、二つの場所に出てくる：")
print("    理論の側： 記述を log₂G ビット短くする")
print("    実験の側： 観測の感度を G 倍に増幅する")
print("  %-34s %-12s %-16s %s" % ("場面", "G", "短くなる[bit]", "増幅"))
for lab, G in [("Λ_QCD を M_P から作る", G_qcd), ("μ が α_s の変化を見る（番外編①）", G_mu)]:
    print("  %-34s %-12.2f %-16.2f %.1f 倍" % (lab, G, math.log2(G), G))
print("  ★ **同じ数が、番外編①では増幅率、番外編②では圧縮率だった。**")
print("     → **安く説明できる階層は、必ず測りやすい。** 逆も真。")
print("     宇宙定数問題は**説明も高く、測りにくい** ── これは二つの事実ではなく、一つ。")
print("")
print("  帰結：もし宇宙定数に指数機構があるなら、利得は G = %.0f 必要。" % (B * ln2))
print("        → **その機構は必然的に w(z) に %.0f 倍の感度を持つ**。" % (B * ln2))
print("        つまり **安い説明は、必ず反証可能**。逃げ場が無い。")

print("")
print("=== ⑩ 正直な線 ===")
print("  ・①の『圧縮はゼロ』は厳密（全単射な写像と誘導事前を仮定したとき）。")
print("  ・②の『α は O(1)』には理由があるが、**どこまで狭いかは規約**。")
print("    [0,1] と取るか [0.001,1] と取るかで log₂B の絶対値は動く（第48回②と同じ）。")
print("  ・⑤の採点表は 5 例だけ。『実際の扱われ方』は文献の空気の要約。")
print("  ・⑦の『手は三つしかない』は**証明ではない**。")
print("    見つけた三つを並べただけで、四つ目が無いことは示していない。")
print("  ・⑨の『安い説明は必ず反証可能』は、**指数機構に限った話**。")
print("    緩和（③）はこの論法の外にある。")
